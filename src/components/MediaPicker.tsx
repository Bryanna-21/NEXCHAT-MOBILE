import React from "react";import {Alert,Text,TouchableOpacity,View} from "react-native";import {pickMedia} from "../core/media";import {Attachment} from "../core/store";
export function MediaPicker({onSelected}:{onSelected:(items:Attachment[])=>void}){return <View style={{flexDirection:"row",gap:8}}><TouchableOpacity onPress={async()=>{try{onSelected(await pickMedia(true))}catch(e){Alert.alert("Media unavailable",e instanceof Error?e.message:"Unable to access media.")}}} style={{padding:10,borderRadius:12,borderWidth:1,borderColor:"#78909C"}}><Text>＋ Photos / Videos</Text></TouchableOpacity></View>}
